## Local Development

Running app:
```bash
> cd app/
> npm install
> npm start
```

Running Firebase emulator:
```bash
> cd firebase
> npm install
> npm run build
> cd ..
> firebase emulators:start
```

Running IPFS node:
```bash
> cd ipfs_node
> npm install
> npm run build
> npm start
```

